<!-- <div class="container">
  <ul class="list-inline">
    <li><a href="/">Home</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
  <ul class="list-inline">
  {{#if user}}
    <li>
      {{#if user.picture}}
        <img class="avatar" src="{{user.picture}}">
      {{else}}
        <img class="avatar" src="{{user.gravatar}}">
      {{/if}}
      {{#if user.name}}
        {{user.name}}
      {{else}}
        {{user.email}}
      {{/if}}
    </li>
    <li><a href="/account">My Account</a></li>
    <li><a href="/logout">Logout</a></li>
  {{else}}
    <li><a href="/login">Log in</a></li>
    <li><a href="/signup">Sign up</a></li>
  {{/if}}
  </ul>
</div> -->
<header>
  <nav class="navbar grey darken-4" role="navigation">
    <a href="#" data-activates="slide-out" class="button-collapse"><i class="fa fa-bars" aria-hidden="true"></i></a>
    <div class="nav-wrapper container">
      <img src="images/szczebrzeszyn-emblem.png" alt="szczebrzeszyn-emblem" class="emblem responsive-img left">
      <a id="logo-container" href="/" class="brand-logo">Jakub Cebrykow</a>
      <!-- <ul class="right hide-on-med-and-down">
         <li><a href="#!">Poparło mnie już: <span class="badge indigo white-text">1 000 000 osób</span></a></li>
         <li><a href="/login"><i class="fa fa-user" style="font-size: 35px;" aria-hidden="true"></i></a></li>
       </ul> -->
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><a href="#!">Poparło mnie już:<span class="badge indigo white-text">{{count}}</span></a></li>
        <li><a href="/">Główna</a></li>
        <li><a href="/contact">Kontakt</a></li>
        {{#if user}}
          <li><a href="/login">Zaloguj</a></li>
          <li><a href="/signup">Rejestracja</a></li>
        {{/if}}
        <!-- {{#if user}}
          <li>
            {{#if user.picture}}
              <img class="avatar responsive-img circle" width="50" height="50" src="{{user.picture}}">
            {{else}}
              <img class="avatar responsive-img circle" width="50" height="50" src="{{user.gravatar}}">
            {{/if}}
            {{#if user.name}}
              {{user.name}}
            {{else}}
              {{user.email}}
            {{/if}}
          </li>
          <li><a href="/account">Moje konto</a></li>
          <li><a href="/logout">Wyloguj</a></li>
        {{else}}
          <li><a href="/login">Zaloguj</a></li>
          <li><a href="/signup">Rejestracja</a></li>
        {{/if}} -->
      </ul>
    </div>
  </nav>
</header>
<!-- Side-Nav -->
<ul id="slide-out" class="side-nav">
  <li>
    <div class="user-view">
      <li><a href="#!">Poparło mnie już:<span class="badge indigo white-text">{{count}}</span></a></li>
      <li>
        <div class="divider"></div>
      </li>
      {{#if user}}

        {{#if user.picture}}
          <!-- <img class="avatar responsive-img circle" width="50" height="50" src="{{user.picture}}"> -->
          <span> <a href="#!user"><img class="avatar responsive-img circle" src="{{user.picture}}"></a></span>
        {{else}}

          <a href="#!user"><img class="avatar responsive-img circle" src="{{user.gravatar}}"></a>
        {{/if}}
        {{#if user.name}}
          <li><a href="#!name"><span class="black-text name">Zalogowany jako: {{user.name}}</span></a></li>
        {{else}}
          <a href="#!email"><span class="black-text email">{{user.email}}</span></a>
        {{/if}}

        <li>
          <div class="divider"></div>
        </li>
        <li><a href="/account"><i class="fa fa-user" aria-hidden="true"></i>Moje konto</a></li>
        <li><a href="/logout"><i class="fa fa-sign-out" aria-hidden="true"></i>Wyloguj</a></li>
      {{else}}
        <li><a href="/login"><i class="fa fa-sign-in" aria-hidden="true"></i>Zaloguj</a></li>
        <li><a href="/signup"><i class="fa fa-user-plus" aria-hidden="true"></i>Rejestracja</a></li>
      {{/if}}
    </div>
  </li>
  <li><a href="#!"><i class="material-icons">cloud</i>First Link With Icon</a></li>
  <li><a href="#!">Second Link</a></li>
  <li>
    <div class="divider"></div>
  </li>
  <li><a class="subheader">Subheader</a></li>
  <li><a class="waves-effect" href="#!">Third Link With Waves</a></li>
</ul>
